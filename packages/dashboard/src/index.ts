/**
 * @userigor/dashboard
 *
 * Local web console for userigor. Use the `rigor-dashboard` binary, or
 * embed the server programmatically via `startServer()` / `buildServer()`.
 */
export { startServer, buildServer } from './server.js';
export type { RunningServer } from './server.js';
