/**
 * @userigor/cursor
 *
 * Renders active userigor patterns as a Cursor rules file.
 * Use `rigor-cursor` to write to `.cursorrules` in the cwd, or
 * import `renderCursorRules` to produce the text yourself.
 */
export { renderCursorRules } from './render.js';
export type { RenderOptions } from './render.js';
