---
description: Recall relevant patterns from past corrections before generating code.
argument-hint: [task description]
---

Use the `rigor_recall` tool with the prompt:

$ARGUMENTS

If patterns are returned, list each by name with its similarity score and impact, then fold the guidance into your next generation. If no patterns are returned, just acknowledge "no relevant patterns" and proceed normally.
