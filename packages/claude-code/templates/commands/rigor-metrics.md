---
description: Show current userigor metrics.
argument-hint: [days, default 30]
---

Call the `rigor_metrics` tool with `{ "days": $ARGUMENTS }` (default 30 if no argument). Render the result as a brief table. Do not editorialize the numbers — let them speak.
